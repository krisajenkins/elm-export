decodePost : Decoder Post
decodePost = Post
  `map`   ("id" := int)
  `apply` ("name" := string)
  `apply` ("age" := maybe float)
  `apply` ("comments" := list decodeComment)
  `apply` ("promoted" := maybe decodeComment)
  `apply` ("author" := maybe string)
